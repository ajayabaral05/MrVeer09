# Grow With Patience Landing Page

Upload all files in this folder to the GitHub repository root.

Files:
- index.html
- style.css
- script.js
- photo 12 .jpg (if included)

The existing Meta Pixel initialization and tracking code in `index.html` is preserved.
The Telegram CTA remains manual-click only, and the existing Subscribe tracking remains in `script.js`.

The page now rotates its gradient theme automatically every 3 seconds with smooth transitions.
